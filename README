该工程的目的是使用同一份程序代码配合多份不同的ANT配置脚本和不同的资源文件夹，执行一条命令便可导出多份不同包名、不同资源、不同广告商的程序包。

关键目录文件说明：
1. AntFiles下面放置的是其中v_xxx.xml是针对某个特定版本的ANT属性配置文件，其他都是公共文件无需做修改；
2. ManifestFiles下各文件夹放置的是不同版本程序用到的AndroidManifest.xml文件；
3. local.properties中SDK的目录需要根据本机的情况作调整。
4. Resource 下放置的是各版本的资源文件

需要库工程 Android Pipeline Lib 的支持：
https://github.com/saubcy/Android-Pipeline-Lib